requestor:
  replicaCount: 2
  enabled: true
  image:
    repository: "quay.io/cdis/requestor"
    tag: "master"
  externalSecrets:
    pushSecret: true
    dbcreds: "ci-perf-requestor-creds"
  postgres:
    dbCreate: true
